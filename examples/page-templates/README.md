# 📄 페이지 템플릿

Designbase UI 라이브러리를 활용한 실제 페이지 템플릿 모음입니다. 컴포넌트 예제와 별개로 실행되는 완전한 페이지 구조입니다.

## 🎯 특징

- ✅ **완전한 페이지 구조** - 실제 프로덕션에서 바로 사용 가능
- ✅ **재사용 가능** - 각 템플릿을 복사하여 프로젝트에 활용
- ✅ **독립 실행** - 컴포넌트 예제와 별도의 포트에서 실행
- ✅ **반응형 디자인** - 모바일부터 데스크톱까지 지원

## 📁 템플릿 목록

- **[dashboard](./dashboard/)** - 대시보드 페이지 템플릿 (포트: 5175)

## 🚀 실행 방법

### 1. 특정 템플릿 실행

```bash
# 대시보드 템플릿으로 이동
cd examples/page-templates/dashboard

# 의존성 설치 (처음 한 번만)
npm install

# 개발 서버 실행
npm run dev
```

브라우저에서 자동으로 열리거나, 표시된 포트 (예: `http://localhost:5175`)로 접속하세요.

### 2. 빌드 및 배포

```bash
# 프로덕션 빌드
npm run build

# 빌드 결과 미리보기
npm run preview
```

## 📦 템플릿 구조

각 템플릿은 다음 구조를 따릅니다:

```
템플릿명/
├── package.json          # 프로젝트 설정
├── vite.config.ts        # Vite 설정 (포트 포함)
├── tsconfig.json         # TypeScript 설정
├── index.html            # HTML 엔트리 포인트
├── README.md            # 템플릿 설명 및 사용법
├── src/
│   ├── main.tsx         # React 엔트리 포인트
│   ├── App.tsx          # 메인 앱 컴포넌트
│   ├── App.scss         # 전역 스타일
│   └── components/      # 페이지 컴포넌트들
└── .gitignore
```

## 💡 활용 방법

1. **직접 사용**: 템플릿을 복사하여 프로젝트에 바로 사용
2. **커스터마이징**: 각 컴포넌트를 수정하여 원하는 디자인으로 변경
3. **학습 자료**: 실제 페이지 구조를 학습하는 데 활용
4. **프로토타입**: 빠른 프로토타이핑에 활용

## 🔧 포트 번호

각 템플릿은 고유한 포트를 사용합니다:

- `dashboard`: 5175

포트는 `vite.config.ts`에서 변경할 수 있습니다.

## 📚 참고 자료

- [Designbase UI 문서](../../packages/ui/README.md)
- [아이콘 가이드](../../packages/icons/README.md)
- [컴포넌트 예제](../design-examples/README.md) - 컴포넌트별 사용 예제

